<h1>Skills</h1>
<%= link_to "New", new_admin_skill_path, class: "btn btn-primary" %>

<% @skills.each do |skill| %>
  <div class = "panel panel-default">
    <div class = "panel-heading">
      <h3 class ="panel-title"><%= skill.name %></h3>
      </div>

      <div class="panel-body">
        <p><%= skill.rating %></p>
        </div>

        <div class="panel-footer">
          <%= link_to "View more details", admin_skill_path(skill), class: "btn btn-primary" %>
           <%= link_to "Edit", edit_admin_skill_path(skill), class: "btn btn-primary" %>
           <%= link_to "New", new_admin_skill_path, class: "btn btn-primary" %>
          </div>
          </div>
          <% end %>