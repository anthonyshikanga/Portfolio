<%= render "layouts/errors", :object => @comment %>

<%= form_for [@post,@comment] do |f| %>
<div class="input-field">
  <%= f.label :comment %>
  <%= f.text_field :comment, required:'required' %>
</div>
<%= f.submit 'Submit' , class:"btn bread-crumb white-text" %>
<% end %>